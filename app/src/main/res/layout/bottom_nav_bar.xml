<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintBottom_toBottomOf="parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="-39dp">
        <ImageButton
            android:id="@+id/openCameraButton"
            android:layout_width="1dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@null"
            android:onClick="navClick"
            app:srcCompat="@drawable/icon_camera_button" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/empty_bucket_screen_menu_bar">
        <ImageButton
            android:id="@+id/greyBucket"
            android:layout_width="1dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_gravity="left"
            android:onClick="navClick"
            app:srcCompat="@drawable/green_bucket"
            android:background="@null"/>

        <ImageButton
            android:id="@+id/donothing"
            android:layout_width="1dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginTop="-16dp"
            android:background="@null" />

        <ImageButton
            android:id="@+id/cameraRoll"
            android:layout_width="1dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:onClick="navClick"
            app:srcCompat="@drawable/green_film_roll"
            tools:background="@null"
            android:background="@null"/>
    </LinearLayout>

</LinearLayout>